package com.web3j.blockchain.automotive.ethereum.contract;

/**
 * Created by gunicolas on 3/08/16.
 */
public class Contract {

/*    private JSONArray abi;

    private String address;
    private String transactionHash;

    private Map<Integer,SolidityFunction> functions;

    public Contract(JSONArray abi, Map<Integer, SolidityFunction> functions) {
        this.abi = abi;
        this.functions = functions;
    }

    public Contract(JSONArray abi, String address, String transactionHash, Map<Integer,SolidityFunction> functions) {
        this.address = address;
        this.transactionHash = transactionHash;
        this.functions = functions;

        for(SolidityFunction f : this.functions.values()){
            f.setAddress(this.address);
        }

    }

    protected SolidityFunction getFunction(String signature){
        return this.functions.get(signature.hashCode());
    }*/
}
